# Rust build output
/target

# Node dependencies (any depth)
node_modules/

# Front-end / Tauri build output
/web-ui/dist/
/web-ui/build/
/web-ui/.env
/tauri-app/dist/
/tauri-app/src-tauri/target/

# Bridge build output and local state
bridges/*/dist/
bridges/*/~/

# Release artifacts
/dist/*.tar.gz
/dist/agent-gnu
/dist/agent-musl
/agent

# Local config / secrets
docker/config/
.env

# Agent session / memory data (may contain project details)
.agent/
